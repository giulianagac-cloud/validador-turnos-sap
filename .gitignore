# Excel con datos reales — nunca commitear
*.xlsx
*.XLSX
*.xls
*.XLS

# Tablas SAP persistidas localmente — nunca commitear
data_local/

# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
.env
*.egg-info/
dist/
build/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# PyInstaller — salida del empaquetado del .exe (build/ y dist/ ya cubiertos
# arriba). El .spec SÍ se versiona (es la config de build, hecha a mano).
*.manifest
!validador.spec

# OS
.DS_Store
Thumbs.db
